// Define the ClassRoom class and export it as default
export default class ClassRoom {
  // Constructor to initialize maxStudentsSize
  constructor(maxStudentsSize) {
    // Assign the argument to the private attribute
    this._maxStudentsSize = maxStudentsSize;
  }
}
